{
  "name": "banaan",
  "license": "MIT",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "release": "release-semver --prefix release",
    "patch-release": "release-semver --sourceBranch release --prefix release --commands [tag,push]"
  },
  "devDependencies": {
    "@topicusonderwijs/release-semver": "../release-semver"
  }
}
